/**
 * Site-wide constants. This is the single source of truth for the production
 * URL. Update BASE_URL here if the domain ever changes - layout.tsx, sitemap.ts,
 * and any other server-side files import from here.
 *
 * Note: public/robots.txt is a static file and cannot import from JS modules,
 * so its Sitemap URL must be kept in sync manually when the domain changes.
 */
export const BASE_URL = "https://raphaelonana.dev"
